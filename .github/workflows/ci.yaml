name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  ci:
    uses: kokajikia/ci-templates/.github/workflows/pipeline.yaml@main
    with:
      config-path: .pipeline.yaml
    secrets: inherit
